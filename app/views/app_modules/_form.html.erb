<%= form_with(model: app_module, local: true) do |form| %>
  <% if app_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(app_module.errors.count, "error") %> prohibited this app_module from being saved:</h2>

      <ul>
      <% app_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :app_module_name %>
  </div>

  <div class="field">
    <%= form.label :controller %>
    <%= form.text_field :controller, id: :app_module_controller %>
  </div>

  <div class="field">
    <%= form.label :actions %>
    <%= form.text_field :actions, id: :app_module_actions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
